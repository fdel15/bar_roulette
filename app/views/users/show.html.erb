<div class="row">
	<div class="col-sm-12 col-lg-12">
		<div class="panel" id="map">
			<div class="panel-body">
				<article>
				</article>
			</div>
		</div>
		<%= form_tag("/random", method: "get") do %>
		  <%= hidden_field :coordinates , :latitude, id:'start_latitude'%>
		  <%= hidden_field :coordinates , :longitude, id:"start_longitude" %>
		  <%= submit_tag("Hop To Bar" , class:"btn btn-default btn-lg", :id=> "barHop") %>
		<% end %>
	</div>
</div>